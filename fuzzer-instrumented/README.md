# Instrumented Fuzzer

- `src/harness.c`            — fork-mode harness
- `src/harness_persistent.c` — persistent-mode harness  
- `src/Makefile`             — build targets: build, fuzz, clean
- `findings/`                — AFL++ output (gitignored)
- `plot_output/`             — graphs from afl-plot
