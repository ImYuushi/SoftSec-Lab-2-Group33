#include <png.h>



int main(int argc, char *argv[]) {
    // Get the path into a variable

    // Read file into png_file (or at least get a pointer to it?)

    // initialize png_struct or pointer to it?
    
    // Initialize png_info or pointer to it?

    // handle setjmp with invalid inputs

    // clean up because theres no trashman in C

}